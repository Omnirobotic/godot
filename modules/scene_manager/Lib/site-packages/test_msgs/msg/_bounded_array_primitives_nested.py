# generated from rosidl_generator_py/resource/_msg.py.em
# generated code does not contain a copyright notice

from copy import copy
import logging
import traceback


class Metaclass(type):
    """Metaclass of message 'BoundedArrayPrimitivesNested'."""

    _CREATE_ROS_MESSAGE = None
    _CONVERT_FROM_PY = None
    _CONVERT_TO_PY = None
    _DESTROY_ROS_MESSAGE = None
    _TYPE_SUPPORT = None

    __constants = {
    }

    @classmethod
    def __import_type_support__(cls):
        try:
            from rosidl_generator_py import import_type_support
            module = import_type_support('test_msgs')
        except ImportError:
            logger = logging.getLogger('rosidl_generator_py.BoundedArrayPrimitivesNested')
            logger.debug(
                'Failed to import needed modules for type support:\n' + traceback.format_exc())
        else:
            cls._CREATE_ROS_MESSAGE = module.create_ros_message_msg__msg_bounded_array_primitives_nested
            cls._CONVERT_FROM_PY = module.convert_from_py_msg__msg_bounded_array_primitives_nested
            cls._CONVERT_TO_PY = module.convert_to_py_msg__msg_bounded_array_primitives_nested
            cls._TYPE_SUPPORT = module.type_support_msg__msg_bounded_array_primitives_nested
            cls._DESTROY_ROS_MESSAGE = module.destroy_ros_message_msg__msg_bounded_array_primitives_nested
            from test_msgs.msg import BoundedArrayPrimitives
            if BoundedArrayPrimitives.__class__._TYPE_SUPPORT is None:
                BoundedArrayPrimitives.__class__.__import_type_support__()

    @classmethod
    def __prepare__(cls, name, bases, **kwargs):
        # list constant names here so that they appear in the help text of
        # the message class under "Data and other attributes defined here:"
        # as well as populate each message instance
        return {
        }


class BoundedArrayPrimitivesNested(metaclass=Metaclass):
    """Message class 'BoundedArrayPrimitivesNested'."""

    __slots__ = [
        '_bounded_array_primitive_values',
    ]

    _fields_and_field_types = {
        'bounded_array_primitive_values': 'test_msgs/BoundedArrayPrimitives[<=3]',
    }

    def __init__(self, **kwargs):
        assert all('_' + key in self.__slots__ for key in kwargs.keys()), \
            'Invalid arguments passed to constructor: %s' % \
            ', '.join(sorted(k for k in kwargs.keys() if '_' + k not in self.__slots__))
        self.bounded_array_primitive_values = kwargs.get('bounded_array_primitive_values', [])

    def __repr__(self):
        typename = self.__class__.__module__.split('.')
        typename.pop()
        typename.append(self.__class__.__name__)
        args = [s[1:] + '=' + repr(getattr(self, s, None)) for s in self.__slots__]
        return '%s(%s)' % ('.'.join(typename), ', '.join(args))

    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        if self.bounded_array_primitive_values != other.bounded_array_primitive_values:
            return False
        return True

    @classmethod
    def get_fields_and_field_types(cls):
        return copy(cls._fields_and_field_types)

    @property
    def bounded_array_primitive_values(self):
        """Message field 'bounded_array_primitive_values'."""
        return self._bounded_array_primitive_values

    @bounded_array_primitive_values.setter
    def bounded_array_primitive_values(self, value):
        if __debug__:
            from test_msgs.msg import BoundedArrayPrimitives
            from collections.abc import Sequence
            from collections.abc import Set
            from collections import UserList
            from collections import UserString
            assert \
                ((isinstance(value, Sequence) or
                  isinstance(value, Set) or
                  isinstance(value, UserList)) and
                 not isinstance(value, str) and
                 not isinstance(value, UserString) and
                 len(value) <= 3 and
                 all(isinstance(v, BoundedArrayPrimitives) for v in value) and
                 True), \
                "The 'bounded_array_primitive_values' field must be a set or sequence with length <= 3 and each value of type 'BoundedArrayPrimitives'"
        self._bounded_array_primitive_values = value
